{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show">
    <h1>{{ 'profile.show.username'|trans }}: {{ user.username }}</h1>
    <h1>{{ 'profile.show.email'|trans }}: {{ user.email }}</h1>
</div>
